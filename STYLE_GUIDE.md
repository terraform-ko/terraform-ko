# 스타일 가이드

## 어조

- 문장은 **"~합니다" 경어체**로 통일합니다.
- 목록(bullet)이나 표 안의 짧은 항목은 경어체를 생략할 수 있습니다.
- 번역투(직역체)를 피하고 자연스러운 한국어 문장으로 옮깁니다.

## 형식

- Markdown/MDX 문법(제목 레벨, 표, 코드 블록, 커스텀 alert 구문 `~>`, `->`, `=>`, `!>` 등)을 원문과 동일하게 유지합니다.
- 문서 제목(h1~h6)의 앵커/슬러그가 깨지지 않도록, 헤딩 자체는 필요 시 원문을 병기합니다. (예: `## 프로바이더 (Providers)`)
- 코드 블록(HCL, CLI 명령어, 파일 경로 등)은 번역하지 않습니다. 코드 안의 주석만 필요하면 한국어로 옮깁니다.
- 링크 경로는 번역본 기준 상대 경로로 유지하되, 대상 문서가 아직 번역되지 않았다면 원문 링크를 유지합니다.

## 용어

- 아래 표에 있는 용어는 그대로 사용합니다. 표에 없는 용어를 새로 정의해야 하면 이 표에 행을 추가하는 PR을 올려주세요.
- 어색하게 번역되는 기술 용어(예: `provider`, `state`, `backend`)는 무리하게 번역하지 않고 원문 또는 한글 음역을 사용합니다.

| 원문 (English) | 번역 (Korean) | 비고 |
| --- | --- | --- |
| Provider | 프로바이더 | 번역하지 않고 음역 |
| Resource | 리소스 | |
| Data Source | 데이터 소스 | |
| Module | 모듈 | |
| State | 스테이트 | "상태"로 번역하지 않음 (state file 등과 혼동 방지) |
| Backend | 백엔드 | |
| Workspace | 워크스페이스 | |
| Provisioner | 프로비저너 | |
| Variable | 변수 | |
| Output | 아웃풋 | 문맥상 "출력값"도 허용 |
| Plan | 플랜 | `terraform plan` 명령어와 개념 모두 지칭 |
| Apply | 적용 | `terraform apply` 명령어 자체는 번역하지 않음 |
| Destroy | 삭제 | |
| Registry | 레지스트리 | |
| HCL (HashiCorp Configuration Language) | HCL | 번역하지 않음 |
| Configuration | 구성 | |
| Lock File | 락 파일 | |
| Drift | 드리프트 | |
