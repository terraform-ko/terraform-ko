export default {
  "index": "Use Vault-backed dynamic credentials in HCP Terraform",
  "aws-configuration": "Use Vault-backed dynamic credentials with the AWS provider in HCP Terraform",
  "gcp-configuration": "Use Vault-backed dynamic credentials with the GCP provider in HCP Terraform",
  "azure-configuration": "Use Vault-backed dynamic credentials with the Azure provider in HCP Terraform"
}
